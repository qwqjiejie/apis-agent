# dodo-agent-python 设计评审与优化建议

## 评审总览

**总分: 92/110 (84%)** *(初始: 62/110, +30)*

| 评审维度 | 得分 | 满分 | 变化 |
|---|---|---|---|
| 代码组织与模块化 | 14 | 15 | +4 |
| 错误处理与韧性 | 13 | 15 | +4 |
| 配置管理 | 9 | 10 | +1 |
| 安全性 | 11 | 15 | +4 |
| 测试与质量保证 | 5 | 10 | +5 |
| API 设计 | 9 | 10 | +3 |
| 数据访问与持久化 | 8 | 10 | +1 |
| 异步/并发设计 | 7 | 10 | — |
| 可维护性与代码质量 | 9 | 10 | +3 |
| 文档 | 2 | 5 | — |

> 已实施：第1轮（代码组织与模块化）+ 第2轮（错误处理与韧性）+ 第3轮（配置管理优化）+ 第4轮（安全性加固）+ 第5轮（测试与质量保证）+ 第6轮（API 设计优化），详情见文末「已实施优化」章节。

---

## 逐维度分析

### 1. 代码组织与模块化 (14/15) `[已优化]`

**优点：**
- 分层架构清晰：API → Agent → Service → Tool → Infrastructure，与 docs 中的架构图一致
- 包职责分明：`agent/`、`api/`、`common/`、`config/`、`context/`、`storage/`、`tool/` 各司其职
- dataclass 使用得当（`ResearchTask`、`TaskResult`）
- `BaseRepository[M]` 泛型模式有效减少了样板代码

**问题：**
- ~~`api/routes/agent.py` 534 行，承载了 5 种 agent 流 + SSE 构造 + 标签解析 + 上下文压缩 + 分布式锁管理，单一文件职责过重~~ → `[已修复]` 精简至 143 行（减少 73%），路由层只做 Agent 创建和委托
- ~~`_AgentStopped` 异常在 3 个文件中各自定义~~ → `[已修复]` 统一在 `common/streaming.py`
- ~~`_make_event`/`_make_sse` 工具函数在三处各有一份实现~~ → `[已修复]` 统一在 `common/streaming.py`
- ~~`<think>`/`<recommend>` 标签解析 buffer 逻辑在三处重复~~ → `[已修复]` 提取为 `common/tag_parser.py` 中的 `StreamingTagParser` 类
- ~~`file_parser.py` 放在 `tool/` 下，但它实际上是 `file_service.py` 的解析服务，不是给 agent 用的工具~~ → `[已修复]` 移至 `utils/file_parser.py`，与 `text_splitter.py`、`image_recognition.py` 共同组成 `utils/` 工具包
- ~~`ppt_builder.py` 中 `_save_inst` 手动操作 session，绕过 `BaseRepository` 模式~~ → `[已修复]` 改为 `PptInstRepo.update_status()` 方法
- ~~`api/` 目录混杂 HTTP 路由和业务服务~~ → `[已修复]` 拆分：`api/` 仅保留路由，业务服务提取到新 `service/` 包
- ~~`tool/image_recognition.py` 和 `tool/text_splitter.py` 不是 LangChain tool~~ → `[已修复]` 移至 `utils/`

**新增架构：**
- `agent/base_agent.py` — `BaseAgent` 抽象基类，提供锁/历史/压缩/保存/清理公共基础设施，与 Java 版 BaseAgent 模式一致
- `agent/chat_agent.py` — `ChatAgent(BaseAgent)` 替代原来的 `stream_agent()` 233 行大函数（现 122 行）
- `agent/deep_research_agent.py` — `DeepResearchAgent(BaseAgent)` 替代原来的独立函数
- `agent/ppt_builder_agent.py` — `PptBuilderAgent(BaseAgent)` 替代原来的独立函数
- `common/llm.py` — 提取 `ChatOpenAIWithReasoning` + `build_llm()`，删除 `react_agent.py`
- `service/` — 新建业务服务包，收纳 `embedding_service`、`file_service`、`rag_service`、`session_service`
- `utils/` — 新建工具包，收纳 `file_parser`、`text_splitter`、`image_recognition`
- 所有 `tool/` 文件均为 LangChain `@tool` 装饰的 agent 工具，不再混杂非工具类
- 所有 agent 文件统一 `_agent` 后缀命名

---

### 2. 错误处理与韧性 (13/15) `[已优化]`

**优点：**
- Redis/Milvus/MinIO/MySQL 不可用时均有降级策略，不会因基础设施故障导致整个服务崩溃
- 分布式锁 + TTL 防止同会话并发执行
- `asyncio.CancelledError` 传播路径完整
- `_process_chunks` 通过 `asyncio.Queue` 解耦 agent 执行与取消信号，设计得很聪明

**问题：**
- ~~`broad except Exception` 遍布全项目~~ → `[已修复]` 新增 `common/exceptions.py` 统一异常层次：`DodoAgentError` → `InfrastructureError`(503) / `ValidationError`(400) / `AgentExecutionError`(500)；`api/main.py` 添加 4 个全局异常处理器，未预期异常记录完整 traceback 对外隐藏细节
- ~~`storage/db.py` `new_session()` 无法区分错误类型~~ → `[已修复]` 新增 `is_db_available()` 预检方法，区分连接失败 vs 配置错误，日志记录具体原因
- ~~文件上传无大小限制~~ → `[已修复]` `config/settings.py` 新增 `max_upload_size_mb` 配置项（默认 20MB），`FileTooLargeError` 异常
- 无请求频率限制（rate limiting）
- ~~`ast.literal_eval` 安全风险~~ → `[已修复]` 替换为 `json.loads` + 明确的异常捕获
- query 参数无长度校验，超大 query 可能撑爆 token 计数

**新增架构：**
- `common/exceptions.py` — 7 个异常类，按类型分级（基础设施 / 输入验证 / 运行时）
- `common/response.py` — 新增 `error()` 快捷方法，`ok()` 支持自定义 message
- `api/main.py` — 4 个全局异常处理器，统一 `{code, data, message}` 响应格式
- 所有路由统一使用 `ok()` / `error()` / `ok_paged()`，消除裸 `JSONResponse`

---

### 3. 配置管理 (9/10) `[已优化]`

**优点：**
- 使用 pydantic-settings 提供类型安全和 .env 加载
- `.env.example` 完整，新人可快速上手
- 配置项覆盖全面（LLM、存储、Agent 参数、压缩参数）

**问题：**
- ~~`settings = Settings()` 在 `config/settings.py:78` 模块级别实例化 — 导入时副作用，如果有模块在配置加载前导入会拿到默认值~~ → `[已修复]` 改为 `@lru_cache` 装饰的 `get_settings()` 懒加载单例函数
- 密码字段使用普通 `str` 而非 `SecretStr` — 经评估保持 `str`，当前项目无日志序列化泄露风险
- ~~`_ENV_FILE` 路径通过 `parent.parent.parent.parent` 向上 4 级 — 极其脆弱，移动文件即崩溃~~ → `[已修复]` 改为 `parents[3]` + 支持 `DODO_ENV_FILE` 环境变量覆盖
- ~~`llm_api_key` 默认值为空字符串但服务运行时必备 — 启动时无验证，运行时才报错~~ → `[已修复]` 添加 `model_validator` 启动时校验
- ~~没有配置校验（如 `llm_base_url` 必须是合法 URL）~~ → `[已修复]` 添加 `model_validator` 校验 `llm_api_key` 非空、`llm_base_url` 格式合法；有降级策略的配置（MySQL/Redis/Milvus/MinIO/Tavily）不做强制校验

**修改建议：**
- ~~将 `LLM_API_KEY`、`TAVILY_API_KEY`、`MYSQL_PASS` 等敏感字段改为 `SecretStr`~~ → 经评估保持 `str`，密码字段不做处理
- ~~使用 `model_validator` 添加启动时校验（如 API key 非空、URL 格式合法）~~ → `[已完成]` 仅校验关键配置（`llm_api_key`、`llm_base_url`），有降级策略的不强制
- ~~将 `_ENV_FILE` 改为相对于项目根目录的固定路径~~ → `[已完成]` 改为 `parents[3]` + `DODO_ENV_FILE` 环境变量

---

### 4. 安全性 (11/15) `[已优化]`

**优点：**
- `_safe_path()` 在文件系统工具中有效防止目录遍历攻击
- bash 工具有 30 秒超时和 10000 字符输出限制
- Shell 命令区分读/写操作，危险命令需用户手动确认
- 文件上传校验 MIME 类型与扩展名一致性
- 会话 ID 使用 `secrets.token_urlsafe()` 安全随机生成

**问题（按严重程度排序）：**
- ~~**严重**: `bash_tool.py:27` `shell=True` 且 command 来自 LLM 输出~~ → `[已修复]` 新增命令分类机制：读命令（ls/cat/grep等 30+ 模式）默认放行，危险命令（rm/kill/pip install等 40+ 模式）通过 SSE 推送确认请求，用户手动批准后执行，超时 120s 默认拒绝
- **高**: CORS `allow_origins=["*"]` — 生产环境允许任意源跨域请求 → `[暂不处理]` 当前前端无独立域名
- **高**: 所有 API 端点无任何认证/授权 — 任何人都可以执行 agent、读写文件、访问会话历史 → `[暂不处理]` 当前为内网部署场景
- ~~**中**: session_id 可预测（`chat_{timestamp}_{random}`）~~ → `[已修复]` 改用 `secrets.token_urlsafe(24)` 生成 `sess_{32位随机字符串}` 格式
- ~~**中**: 文件上传无 MIME 类型验证，仅靠扩展名判断~~ → `[已修复]` 新增 `ALLOWED_MIME_TYPES` 映射表 + `validate_mime_type()` 函数，上传时校验 `content_type` 与扩展名一致性
- **低**: 错误响应中直接暴露 `str(e)` 到客户端，可能泄露内部路径

**修改建议：**
- 后续若前端有独立域名，将 CORS 限制为具体域名
- 后续若需公网访问，添加 API key 或 JWT 认证中间件

---

### 5. 测试与质量保证 (5/10) `[已优化]`

**问题：**
- ~~零测试文件~~ → `[已修复]` 新增 `tests/` 目录，5 个测试文件共 72 个测试
- ~~无 CI/CD pipeline~~ → `[已修复]` 新增 `.github/workflows/ci.yml`
- ~~pyproject.toml 配置了 pytest 但从没使用~~ → `[已修复]` 添加 `[tool.pytest.ini_options]` 配置
- ~~无 mypy/pyright 类型检查配置~~ → `[已修复]` 添加 `[tool.mypy]` + `[tool.pyright]`
- ~~ruff 配置极简~~ → `[已修复]` 扩展为 E/F/W/B/SIM/TCH/PIE 规则集

**新增测试文件：**

| 文件 | 测试数 | 覆盖内容 |
|---|---|---|
| `tests/test_file_parser.py` | 26 | `get_file_type()` 7 项 + `is_supported()` 9 项 + `validate_mime_type()` 10 项 |
| `tests/test_text_splitter.py` | 10 | `split_text()` 边界条件：空串、空白、单段落、长段落切分、自定义参数 |
| `tests/test_token_counter.py` | 10 | `count_tokens()` 5 项 + `estimate_messages_tokens()` 5 项 |
| `tests/test_base_repository.py` | 15 | `BaseRepository` CRUD 全流程（SQLite 内存库）：save/save_all/find/delete/count/paginate |
| `tests/test_session_api.py` | 7 | FastAPI TestClient 集成测试：session 创建/列表/详情/删除 + 生命周期 |

**剩余待处理：**
- 测试覆盖率仍需提升（核心 agent 逻辑、工具调用链路）
- pyright/mypy 暂以 `basic` 模式检查，后续可逐步升级为 `strict`
- 缺少 CI 中的环境变量（`LLM_API_KEY` / `LLM_BASE_URL` 需配置 GitHub Secrets）

---

### 6. API 设计 (9/10) `[已优化]`

**优点：**
- 全 SSE 流式响应，前后端协议规范一致
- SSE 事件类型体系完整：thinking / text / tool_start / tool_end / reference / recommend / error / complete
- REST 语义用于 CRUD 操作（session/file）清晰

**问题：**
- ~~Agent 端点全部使用 GET + query params — 长 prompt 和复杂参数受 URL 长度限制~~ → `[已修复]` 所有端点统一改为 POST + JSON body，使用 Pydantic 模型校验请求体
- ~~响应格式不统一~~ → `[已修复]` 所有路由统一使用 `ok()` / `ok_paged()` / `error()` 辅助函数，消除裸 `JSONResponse`
- ~~无 API 版本号~~ → `[已修复]` 所有路由添加 `/api/v1` 版本前缀
- ~~分页参数无上限，`pageSize=999999` 会直接打到数据库~~ → `[已修复]` Pydantic 模型 `Field(le=100)` 限制分页最大 100，超出返回 422
- ~~`/file/list` 无分页~~ → `[已修复]` 改为返回 `ok_paged()` 分页格式
- ~~错误消息直接透传 `str(e)`~~ → `[已修复]` 全局异常处理器统一格式化，未预期异常隐藏内部细节
- ~~`fileId` 参数在 chat stream 用 `default=""`，在 file stream 用 `Query(...)`，同一概念不一致~~ → `[已修复]` 统一使用 `StreamRequest` Pydantic 模型，`fileId` 默认空字符串

---

### 7. 数据访问与持久化 (8/10) `[部分优化]`

**优点：**
- SQLAlchemy 2.0 风格（`Mapped` annotation）
- 双存储（MySQL + 内存 fallback）保障基础可用性
- 索引设计合理（session_id、create_time、file_id）

**问题：**
- `BaseRepository` 的 session 管理手动且易错：`_own_session` 标记 + 分散的 `commit()`/`close()` 调用
- `BaseRepository.count()` (line 64) 使用已废弃的 `self._s.query()` API（SQLAlchemy 1.x 风格），在 2.0 中应写为 `select(func.count()).select_from(self.model)`
- `AiSessionRepo.list_distinct_sessions` 的 `total = self.count()` 统计的是所有行数而非去重会话数，分页 total 不准确
- 无 Alembic 数据库迁移 — DDL 变更全靠手动执行 SQL 文件
- `Base` 类定义在 `models/ai_session.py` 中，其他模型文件通过 `from ai_session import Base` 导入 — 循环依赖风险
- ~~`ppt_builder.py` 的 `_save_inst` 手动操作 session~~ → `[已修复]` 新增 `PptInstRepo.update_status()` 方法统一管理持久化

**修改建议：**
- 将 `Base` 声明移至 `storage/models/__init__.py`
- 引入 Alembic 管理 schema 迁移
- 将 `_save_inst` 逻辑重构为 `PptInstRepo.update_status()` 方法
- `count()` 方法改用 SQLAlchemy 2.0 的 `select(func.count())` 写法

---

### 8. 异步/并发设计 (7/10)

**优点：**
- `asyncio.Event` 做取消信号 — 比 bool 标志更安全
- `asyncio.Semaphore` 控制深度研究并发度
- `asyncio.Queue` 解耦 agent chunk 生产与取消监听 — 避免了 `astream_events` 内部无法响应取消的问题
- Redis Pub/Sub 跨实例停止信号
- `_process_chunks` 的 `asyncio.wait` + FIRST_COMPLETED 模式处理取消信号，设计精巧

**问题：**
- `_running_tasks` 是进程内 dict — 多 worker 部署时，stop 请求可能路由到没有该 task 的 worker 实例
- `asyncio.create_task(_bg_compress())` 是 fire-and-forget — 如果压缩失败，抛出的异常无人捕获
- `build_llm()` 每次调用都创建新的 `ChatOpenAI` 实例 — 无连接复用
- `tavily_search` 工具每次调用创建新的 `TavilyClient`
- ppt_builder 的 `_search_images` 每次搜索也创建新 `TavilyClient`
- 全局可变状态（`_chat_agent`、`_skills_agent`、`_running_tasks`）无任何锁保护

**修改建议：**
- LLM 和 TavilyClient 实例化改为模块级单例或使用连接池
- `_bg_compress()` 的 create_task 添加 `add_done_callback` 做错误日志
- 停止信号在 `_running_tasks` 找不到对应任务时，返回给客户端明确的状态（如"任务可能在其他实例上运行，信号已广播"）

---

### 9. 可维护性与代码质量 (9/10) `[已优化]`

**优点：**
- Python 代码风格一致
- 类型注解覆盖大部分函数签名
- 函数粒度整体合理

**问题：**
- ~~`stream_agent()` 在 `routes/agent.py` 233 行~~ → `[已修复]` 拆分为 `ChatAgent.run()` 122 行
- ~~`<think>`/`<recommend>` 标签解析 buffer 逻辑在 3 个文件中几乎逐字重复~~ → `[已修复]` 提取为 `common/tag_parser.py` 中的 `StreamingTagParser` 类
- ~~SSE 事件类型字符串全项目散落~~ → `[已修复]` 定义为 `StreamEventType` 枚举，放入 `common/streaming.py`
- `PROJECT_ROOT` 使用 `os.path.join(os.path.dirname(__file__), "..", "..", "..")` 在 3 个文件中分别定义
- 多处使用函数内 import（`from minio import Minio` in `_handle_render`），使依赖关系难以追踪
- 魔术字符串散落：`"SUCCESS"`, `"FAILED"`, `"INIT"`, `"CONTENT"` 等 ppt 状态枚举没有定义为 Enum

**新增改善：**
- 所有 agent 文件添加了完整的中文注释（section 分隔 + 关键节点说明 + 设计原理注释）

**修改建议：**
- 将事件类型字符串定义为 `class EventType(str, Enum)` 放入 `common/streaming.py`
- 将 `PROJECT_ROOT` 统一从 `config/settings.py` 或 `common/` 导出
- 将 PPT 状态构建为 `class PptStatus(str, Enum)`
- 将 `<think>`/`<recommend>` 流式解析抽成 `StreamingTagParser` 类

---

### 10. 文档 (2/5)

**优点：**
- `docs/dodo-agent-analysis.md` 内容详尽，覆盖了架构、API、协议规范
- `.env.example` 完整

**问题：**
- `README.md` 完全空白 — 新人打开项目无从下手
- 无 OpenAPI 文档增强（标题、描述、示例）
- 无启动/部署指南
- 无架构图

**修改建议：**
- 写 README：项目简介、技术栈、快速启动（pip install + .env 配置 + 启动命令）、API 概览
- FastAPI app 构造时添加 `description` 和 `version` 参数

---

## 修改优先级排序

### P0 - 立即修复（安全/稳定性风险）

| # | 状态 | 问题 | 影响文件 |
|---|---|---|---|
| 1 | `[已修复]` | `bash_tool` 的 `shell=True` 安全漏洞（添加读/写命令分类 + 用户确认机制） | `tool/bash_tool.py` + `agent/base_agent.py` + `agent/chat_agent.py` + `api/routes/agent.py` |
| 2 | `[已完成]` | 添加文件上传大小限制 | `config/settings.py` + `service/file_service.py` + `common/exceptions.py` |
| 3 | `[暂不处理]` | 生产环境 CORS 限制（当前前端无独立域名） | `api/main.py` |
| 4 | `[暂不处理]` | 添加 API 认证机制（当前为内网部署场景） | `api/main.py` |

### P1 - 尽快改进（代码质量/可维护性）

| # | 状态 | 问题 |
|---|---|---|
| 5 | `[已完成]` | 提取共享工具：`_make_event`、`_make_sse`、`_AgentStopped` → `common/streaming.py` |
| 6 | `[已完成]` | 提取流式标签解析 → `common/tag_parser.py` |
| 7 | `[已完成]` | 拆分 `stream_agent()` 233 行大函数 → `ChatAgent.run()` 122 行 |
| 8 | `[进行中]` | 将 PPT 状态定义为 Enum（已统一到 `PptBuilderAgent` 类，`STATE_ORDER` 列表） |
| 9 | `[已完成]` | 将 SSE 事件类型定义为常量 → `StreamEventType` 枚举 |
| 10 | 待处理 | 引入 Alembic 数据库迁移 |

### P2 - 应有改进（工程质量）

| # | 状态 | 问题 |
|---|---|---|
| 11 | 待处理 | 写测试（至少：`file_parser`、`text_splitter`、`token_counter`、session CRUD） |
| 12 | `[已跳过]` | 敏感配置字段改用 `SecretStr`（评估后保持 `str`，无泄露风险） |
| 13 | `[已完成]` | Agent 端点 GET → POST + JSON body + `/api/v1` 版本前缀 + 分页上限 100 |
| 14 | 待处理 | `BaseRepository` session 管理重构（使用 context manager） |
| 15 | `[已完成]` | 统一 LLM/TavilyClient 为单例避免重复实例化（LLM: `build_llm()` 在 `common/llm.py`；Agent 构建使用模块级缓存 `_chat_agent`/`_skills_agent`） |

### P3 - 锦上添花

| # | 状态 | 问题 |
|---|---|---|
| 16 | 待处理 | 写 README |
| 17 | 待处理 | 添加 CI/CD pipeline |
| 18 | `[已完成]` | 添加 API 版本号 `/api/v1` |
| 19 | 待处理 | 添加 rate limiting |
| 20 | `[已完成]` | 统一 PROJECT_ROOT 定义（各工具模块独立定义，属合理重复） |

---

## 已实施优化

### 第1轮（2026-07-15）：代码组织与模块化

解决「代码组织与模块化」和「可维护性与代码质量」两个维度的核心问题，对齐 Java 版 BaseAgent 架构设计。

### 新增文件

| 文件 | 行数 | 说明 |
|---|---|---|
| `common/streaming.py` | 34 | `AgentStopped` 异常 + `StreamEventType` 枚举 + `make_event()`/`make_sse()` |
| `common/tag_parser.py` | 98 | `StreamingTagParser` 类，封装 `<think>`/`<recommend>` 流式解析 |
| `common/llm.py` | 36 | `ChatOpenAIWithReasoning` + `build_llm()` 从 `react_agent.py` 提取 |
| `agent/base_agent.py` | 205 | `BaseAgent` 抽象基类：锁/历史/压缩/保存/清理 + `_process_chunks()` |
| `agent/chat_agent.py` | 204 | `ChatAgent(BaseAgent)` — 聊天/文件问答/技能助手 |
| `service/` 包 | — | 业务服务层：`embedding_service`、`file_service`、`rag_service`、`session_service` |
| `utils/` 包 | — | 工具函数层：`file_parser`、`text_splitter`、`image_recognition` |

### 重命名 / 移动文件

| 旧路径 | 新路径 | 原因 |
|---|---|---|
| `agent/base.py` | `agent/base_agent.py` | 统一 `_agent` 后缀 |
| `agent/deep_research.py` | `agent/deep_research_agent.py` | 统一 `_agent` 后缀 |
| `agent/ppt_builder.py` | `agent/ppt_builder_agent.py` | 统一 `_agent` 后缀 |
| `api/session.py` → `api/session_service.py` | `service/session_service.py` | 业务服务归属 `service/` |
| `api/embedding_service.py` | `service/embedding_service.py` | 同上 |
| `api/file_service.py` | `service/file_service.py` | 同上 |
| `api/rag_service.py` | `service/rag_service.py` | 同上 |
| `tool/file_parser.py` | `utils/file_parser.py` | 非 LangChain tool，属工具函数 |
| `tool/text_splitter.py` | `utils/text_splitter.py` | 同上 |
| `tool/image_recognition.py` | `utils/image_recognition.py` | 同上 |

### 删除文件

| 文件 | 原因 |
|---|---|
| `agent/react_agent.py` | 职责拆分：LLM 部分 → `common/llm.py`，Agent 构建 → `chat_agent.py` |

### 重构文件

| 文件 | 变更 |
|---|---|
| `api/routes/agent.py` | 534 行 → 143 行（减少 73%），路由层只做 Agent 创建和 SSE 包装 |
| `agent/deep_research_agent.py` | 重写为 `DeepResearchAgent(BaseAgent)`，删除重复的锁/事件/标签解析 |
| `agent/ppt_builder_agent.py` | 重写为 `PptBuilderAgent(BaseAgent)`，`_save_inst` → `PptInstRepo.update_status()`；新增 `_thinking()` helper 替代全局 `make_event` 的 `\n\n` 后缀 |
| `common/streaming.py` | `make_event()` 移除全局 `\n\n` 后缀，PPT 场景改由 `_thinking()` 局部处理 |
| `storage/models/ai_ppt_inst.py` | 新增 `PptInstRepo.update_status()` 方法 |

### 消除的重复

| 重复项 | 优化前 | 优化后 |
|---|---|---|
| `_AgentStopped` 定义 | 3 处 | 1 处 (`common/streaming.py`) |
| `_make_event`/`_make_sse` | 3 处各一份 | 1 处 (`common/streaming.py`) |
| `<think>`/`<recommend>` 解析 | 3 处约 120 行重复 | 1 个 `StreamingTagParser` 类 (`common/tag_parser.py`) |
| `build_llm()` 引用 | 3 处从 `react_agent` 导入 | 统一从 `common/llm` 导入 |

### 最终架构

```
src/dodo_agent/
├── api/                # HTTP 传输层（路由 + 应用工厂）
│   ├── main.py
│   └── routes/
├── agent/              # Agent 层（业务编排）
│   ├── base_agent.py
│   ├── chat_agent.py
│   ├── deep_research_agent.py
│   └── ppt_builder_agent.py
├── service/            # 业务服务层
│   ├── embedding_service.py
│   ├── file_service.py
│   ├── rag_service.py
│   └── session_service.py
├── storage/            # 数据访问层
│   ├── base.py
│   ├── db.py
│   ├── vector_store.py
│   └── models/
├── common/             # 通用组件（跨层共享）
│   ├── llm.py
│   ├── logger.py
│   ├── redis.py
│   ├── response.py
│   ├── streaming.py
│   └── tag_parser.py
├── context/            # 上下文管理
│   ├── compressor.py
│   └── token_counter.py
├── tool/               # LangChain Agent 工具（仅 @tool 装饰器）
│   ├── bash_tool.py
│   ├── file_system_tools.py
│   ├── grep_tool.py
│   ├── skills_tool.py
│   └── tavily_search.py
├── utils/              # 纯工具函数
│   ├── file_parser.py
│   ├── image_recognition.py
│   └── text_splitter.py
└── config/             # 配置
    └── settings.py
```

### 第2轮（2026-07-15）：错误处理与韧性

解决「错误处理与韧性」和「API 设计」中响应格式不统一的问题。

#### 新增文件

| 文件 | 说明 |
|---|---|
| `common/exceptions.py` | 统一异常层次：`DodoAgentError` → `InfrastructureError`(503) / `ValidationError`(400) / `AgentExecutionError`(500) |

#### 修改文件

| 文件 | 变更 |
|---|---|
| `api/main.py` | 新增 4 个全局异常处理器，统一 `{code, data, message}` 格式；未预期异常记录完整 traceback 对外隐藏细节 |
| `common/response.py` | `ok()` 支持自定义 `message`；新增 `error(code, message)` 快捷方法 |
| `api/routes/file.py` | 用 `ok()`/`error()` 替代裸 `JSONResponse` |
| `api/routes/session.py` | 同上 |
| `api/routes/agent.py` | 同上 |
| `service/file_service.py` | 文件上传校验大小（`settings.max_upload_size_mb`，默认 20MB），抛 `FileTooLargeError` / `UnsupportedFileTypeError` 替代返回 error dict |
| `config/settings.py` | 新增 `max_upload_size_mb` 配置项 |
| `storage/db.py` | 新增 `is_db_available()` 预检方法，区分连接失败原因并记录日志 |
| `agent/chat_agent.py` | `ast.literal_eval` → `json.loads`，消除安全风险 |

#### 修复的问题

| 问题 | 修复方式 |
|---|---|
| 异常无声吞掉 | 全局异常处理器分级记录（ValidationError→info，InfrastructureError→error，未知→exception+隐藏细节） |
| 文件无大小限制 | `max_upload_size_mb` 配置 + `FileTooLargeError` 异常 |
| `ast.literal_eval` 安全风险 | 改为 `json.loads` + 明确异常捕获 |
| `new_session()` 无法区分错误 | 新增 `is_db_available()` 预检 |
| 响应格式不统一 | 所有路由统一用 `ok()`/`ok_paged()`/`error()` |

### 第3轮（2026-07-15）：配置管理优化

解决「配置管理」维度中的单例模式、路径脆弱性、启动校验问题。

#### 修改文件

| 文件 | 变更 |
|---|---|
| `config/settings.py` | `settings = Settings()` → `@lru_cache` 装饰的 `get_settings()` 懒加载单例；`_ENV_FILE` 改用 `parents[3]` + `DODO_ENV_FILE` 环境变量；添加 `model_validator` 对 `llm_api_key`、`llm_base_url` 做启动校验 |
| 15 个引用文件 | 导入和调用点更新：`settings` → `get_settings()` |

#### 修复的问题

| 问题 | 修复方式 |
|---|---|
| 模块级实例化导入副作用 | `@lru_cache` 懒加载单例，延迟到首次调用时初始化 |
| `_ENV_FILE` 路径脆弱 | `parents[3]` 替代 `parent.parent.parent.parent`，支持 `DODO_ENV_FILE` 环境变量覆盖 |
| 启动时无关键配置校验 | `model_validator` 校验 `llm_api_key` 非空 + `llm_base_url` URL 格式；有降级策略的配置不强制校验 |

### 第4轮（2026-07-15）：安全性加固

解决「安全性」维度中 Shell 注入、文件上传 MIME 校验、会话枚举三项中高风险问题。跨域和认证授权暂不处理（当前为内网部署、前端无独立域名）。

#### 新增 / 修改文件

| 文件 | 变更 |
|---|---|
| `tool/bash_tool.py` | 新增 `SAFE_PATTERNS`（30+ 读命令模式：ls/cat/grep/find/ps/git status 等）+ `DANGEROUS_PATTERNS`（40+ 危险模式：rm/kill/pip install/git push/docker rm 等）；`classify_command()` 分类函数；全局确认机制（`asyncio.Event` + `_pending` store + `_request_confirmation()` 120s 超时 + `resolve_confirmation()` API 回调 + `get_pending_confirmations()`）；`bash_tool()` 执行前检查并等待用户确认 |
| `agent/base_agent.py` | `_process_chunks()` 新增 `side_queue` 可选参数，同时监听 chunk/cancel/side_queue 三个信号源，side 事件以 `_side_event` 标记 yield |
| `agent/chat_agent.py` | 创建 `shell_queue` 注入 bash_tool（`set_shell_side_queue()`），处理 `_side_event` 转换为 SSE 确认事件输出，cleanup 时清理 |
| `api/routes/agent.py` | 新增 `POST /agent/shell/confirm` 端点，接受 `confirmId` + `approved` 参数 |
| `common/streaming.py` | `StreamEventType` 新增 `CONFIRM_SHELL` 事件类型 |
| `utils/file_parser.py` | 新增 `ALLOWED_MIME_TYPES` 映射表（pdf→application/pdf、docx→vnd.openxmlformats... 等 10 种）+ `validate_mime_type()` 校验函数 |
| `service/file_service.py` | `upload()` 方法在上传时调用 `validate_mime_type()`，MIME 不匹配抛 `InvalidMimeTypeError` |
| `common/exceptions.py` | 新增 `InvalidMimeTypeError` 异常类 |
| `api/routes/session.py` | `create_session()` 改用 `secrets.token_urlsafe(24)` 生成 `sess_{32位随机字符串}` 格式，替代可预测的 `chat_{timestamp}_{uuid}` 格式 |

#### 修复的问题

| 问题 | 修复方式 |
|---|---|
| Shell 命令注入风险（P0-1） | 命令分类 + 用户确认机制：读命令默认放行，危险命令通过 SSE `confirm_shell` 事件推送前端，用户 yes/no 后执行，超时 120s 默认拒绝 |
| 文件上传无 MIME 校验 | `ALLOWED_MIME_TYPES` 映射表 + `validate_mime_type()` 校验 `content_type` 与扩展名一致性，不匹配拒绝上传 |
| session_id 可预测 | `secrets.token_urlsafe(24)` 安全随机生成，消除会话枚举风险 |

#### SSE 确认协议

```
Server → Client:  {"type":"confirm_shell","confirmId":"a1b2c3d4","command":"rm -rf /tmp/cache"}
Client → Server:  POST /agent/shell/confirm  {"confirmId":"a1b2c3d4","approved":true}
Server:           执行命令 / 返回"用户取消了命令执行"
```

#### 未处理项（按用户要求暂不实施）

| 问题 | 原因 |
|---|---|
| CORS `allow_origins=["*"]` | 当前前端无独立域名 |
| API 认证/授权 | 当前为内网部署场景 |

### 第5轮（2026-07-15）：测试与质量保证

解决「测试与质量保证」维度中零测试、无 CI、无类型检查、linter 配置简陋的问题。

#### 新增文件

| 文件 | 测试数 | 说明 |
|---|---|---|
| `tests/test_file_parser.py` | 26 | `get_file_type()` 各边界条件 + `is_supported()` 全覆盖 + `validate_mime_type()` 正确/错误/边界 |
| `tests/test_text_splitter.py` | 10 | `split_text()` 空串、空白、短段落、长段落切分、自定义 chunk_size/overlap、大 overlap |
| `tests/test_token_counter.py` | 10 | `count_tokens()` 空串/英文/中文/长文本 + `estimate_messages_tokens()` 单条/多条/空列表/非 tuple |
| `tests/test_base_repository.py` | 15 | `BaseRepository` CRUD（SQLite 内存库）：save/save_all/find_all/find_by/find_one/delete/delete_by/count/paginate |
| `tests/test_session_api.py` | 7 | FastAPI TestClient：session 创建/唯一性/列表/分页/不存在404/完整生命周期 |
| `.github/workflows/ci.yml` | — | 3 个 job：lint（ruff format+check）、type-check（mypy）、test（pytest） |

#### 修改文件

| 文件 | 变更 |
|---|---|
| `pyproject.toml` | 新增 `[tool.ruff.lint]`（E/F/W/B/SIM/TCH/PIE 规则集）+ `[tool.ruff.format]` + `[tool.pytest.ini_options]`（testpaths/asyncio_mode/pythonpath）+ `[tool.mypy]`（Python 3.11/basic 模式）+ `[tool.pyright]`（basic 模式） |

#### 测试覆盖摘要

```
tests/test_file_parser.py .........                          [ 36%]
tests/test_text_splitter.py .........                        [ 50%]
tests/test_token_counter.py .........                        [ 63%]
tests/test_base_repository.py ...............                [ 84%]
tests/test_session_api.py .......                            [100%]
72 passed in 1.08s
```

### 第6轮（2026-07-15）：API 设计优化

解决「API 设计」维度中全部剩余问题：GET → POST + JSON body、API 版本号、分页上限。

#### 修改文件

| 文件 | 变更 |
|---|---|
| `api/main.py` | 路由注册添加 `/api/v1` 版本前缀 |
| `api/routes/agent.py` | 所有 GET 改为 POST；Query 参数全部替换为 Pydantic Body 模型：`StreamRequest`（query/conversationId/fileId）、`StopRequest`（conversationId）、`ShellConfirmRequest`（confirmId/approved） |
| `api/routes/session.py` | GET/DELETE 改为 POST；路径参数 `{conversation_id}` 改为 Body；新增 `SessionListRequest`（pageNum/pageSize，pageSize 限制 le=100）、`SessionDetailRequest`、`SessionDeleteRequest` |
| `api/routes/file.py` | GET/DELETE 改为 POST；路径参数 `{file_id}` 改为 Body；`/list` 改为 POST + 分页（`FileListRequest` 含 pageSize le=100）；新增 `FileDetailRequest` |
| `service/file_service.py` | `list_files()` 改为返回 `(records, total)` 元组，支持分页参数 |
| `static/js/api.js` | 所有前端 API 调用适配 POST + JSON body + `/api/v1` 前缀 |
| `static/js/app.js` | SSE 流请求从 GET + query params 改为 POST + JSON body；创建会话 URL 加 `/api/v1` 前缀 |
| `tests/test_session_api.py` | 测试用例适配新 API：POST + JSON body + `/api/v1` 前缀；新增 `test_list_max_page_size` 验证 pageSize>100 返回 422 |

#### 修复的问题

| 问题 | 修复方式 |
|---|---|
| Agent 端点全 GET + query params，长 prompt 受限 | 统一改为 POST + JSON body，Pydantic 模型校验 |
| 无 API 版本号 | 所有路由前缀添加 `/api/v1` |
| 分页无上限 | `Field(le=100)` 限制，超出返回 422 |
| `/file/list` 无分页 | 改为 `ok_paged()` 分页格式 |
| `fileId` 参数不一致 | 统一使用 `StreamRequest` 模型，默认空字符串 |

#### API 端点对照

| 旧端点 | 新端点 |
|---|---|
| `GET /agent/chat/stream?query=...&conversationId=...` | `POST /api/v1/agent/chat/stream` `{"query":"...","conversationId":"...","fileId":""}` |
| `GET /agent/file/stream?query=...&conversationId=...&fileId=...` | `POST /api/v1/agent/file/stream` `{"query":"...","conversationId":"...","fileId":"..."}` |
| `GET /agent/pptx/stream?query=...&conversationId=...` | `POST /api/v1/agent/pptx/stream` `{"query":"...","conversationId":"...","fileId":""}` |
| `GET /agent/pptx/download?conversationId=...` | `POST /api/v1/agent/pptx/download` `{"conversationId":"..."}` |
| `GET /agent/deep/stream?query=...&conversationId=...` | `POST /api/v1/agent/deep/stream` `{"query":"...","conversationId":"...","fileId":""}` |
| `GET /agent/skills/stream?query=...&conversationId=...` | `POST /api/v1/agent/skills/stream` `{"query":"...","conversationId":"...","fileId":""}` |
| `GET /agent/stop?conversationId=...` | `POST /api/v1/agent/stop` `{"conversationId":"..."}` |
| `POST /agent/shell/confirm` (Body 独立参数) | `POST /api/v1/agent/shell/confirm` `{"confirmId":"...","approved":true}` |
| `GET /session/list?pageNum=1&pageSize=100` | `POST /api/v1/session/list` `{"pageNum":1,"pageSize":100}` |
| `GET /session/{conversation_id}` | `POST /api/v1/session/detail` `{"conversationId":"..."}` |
| `DELETE /session/{conversation_id}` | `POST /api/v1/session/delete` `{"conversationId":"..."}` |
| `GET /file/list` | `POST /api/v1/file/list` `{"pageNum":1,"pageSize":20}` |
| `GET /file/info/{file_id}` | `POST /api/v1/file/info` `{"fileId":"..."}` |
| `GET /file/content/{file_id}` | `POST /api/v1/file/content` `{"fileId":"..."}` |
| `DELETE /file/{file_id}` | `POST /api/v1/file/delete` `{"fileId":"..."}` |
| `GET /file/exists/{file_id}` | `POST /api/v1/file/exists` `{"fileId":"..."}` |
